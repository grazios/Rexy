doctype html
-var projectName = "Rexy"

block vars
  -var pageid = "body"
mixin htmlHeadBody
  head
    meta(charset="UTF-8")

    block title
      title#title #{projectName}
    block base
      base(href="/")

    meta(name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1")
    meta(name="description" content="")
    meta(name="keywords" content="")
    meta(name="author" content="")
    meta(name="generator" content="")

    link(rel="shortcut icon" href="/images/favicon.ico")

    block scripts


    block styles

  body(id="#{pageid}")
    block header
      include header

    div#contents_wrap
      block content

    block footer
      include footer

//if env == "development"
//  html(ng-app="eduma" ng-controller="AppCtrl")
//    mixin htmlHeadBody
//else
html(ng-app="eduma" ng-controller="AppCtrl" translate-cloak)
  mixin htmlHeadBody
